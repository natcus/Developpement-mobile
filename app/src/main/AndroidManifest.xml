<?xml version="1.0" encoding="utf-8"?>
<!--
    Manifest Android de l'application de chat.
    Déclare toutes les activités et la permission Internet requise pour Firebase.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission réseau obligatoire pour Firebase Auth et Firestore -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- Permissions pour le Peer-to-Peer (Nearby Connections) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    
    <!-- Android 12+ (API 31+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" android:usesPermissionFlags="neverForLocation" />

    <application
        android:name=".ChatApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chat"
        tools:targetApi="31">

        <!--
            LoginActivity : point d'entrée de l'application (MAIN + LAUNCHER).
            L'utilisateur voit d'abord cet écran s'il n'est pas connecté.
        -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- RegisterActivity : écran d'inscription -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"/>

        <!-- MainActivity : écran principal après connexion, gère le routage -->
        <activity
            android:name=".MainActivity"
            android:exported="false"/>

        <!-- ChatActivity : écran du chat en temps réel -->
        <activity
            android:name=".ChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>
        <!--
            adjustResize : le layout se redimensionne quand le clavier apparaît,
            pour que la barre de saisie reste visible.
        -->

        <!-- ProfileActivity : écran de gestion de la photo de profil -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false"/>
        <activity
            android:name=".GroupInfoActivity"
            android:exported="false"/>


        <!-- FullScreenImageActivity : visualiseur d'images -->
        <activity
            android:name=".FullScreenImageActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

        <!-- StoryActivity : visualisation des stories -->
        <activity
            android:name=".StoryActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>


        <!-- Service pour recevoir les notifications Push en arrière-plan -->
        <service
            android:name=".service.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Provider pour permettre l'ouverture de fichiers via des Intents externes -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>